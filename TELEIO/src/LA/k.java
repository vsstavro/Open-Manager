package LA;

import javafx.animation.TranslateTransition;
import javafx.scene.paint.Color;
import javafx.scene.shape.Rectangle;
import javafx.util.Duration;

public class k {

	
	
	
	
	
	
/*
	
		
 				
 				
 				
 				
 			
 			
		
		Menu.setOnMouseClicked(event->{
		getChildren().add(m);
		m.getChildren().addAll(Team,Team,Train,Nextweak,Save,btnExit);
		u1.getChildren().clear();
		
		TranslateTransition tt = new TranslateTransition(Duration.seconds(0.5),u1);
		tt.setToX(u1.getTranslateX()- offset);
		
		TranslateTransition tt1 = new TranslateTransition(Duration.seconds(0.25),m);
		tt1.setToX(u1.getTranslateX());
		
		tt.play();
		tt1.play();
		tt.setOnFinished(evt ->{
			getChildren().remove(u1);
			
		});
	});

	/*Save.setOnMouseClicked(event->{
	getChildren().add(menu1);
	menu1.getChildren().addAll(btnSelect,btnBack,btnExit);
	menu0.getChildren().clear();
	
	TranslateTransition tt = new TranslateTransition(Duration.seconds(0.25),menu0);
	tt.setToX(menu0.getTranslateX()- offset);
	
	TranslateTransition tt1 = new TranslateTransition(Duration.seconds(0.5),menu1);
	tt1.setToX(menu0.getTranslateX());
	
	tt.play();
	tt1.play();
	tt.setOnFinished(evt ->{
		getChildren().remove(menu0);
		
	});
	
	
  });*/
	

	
	
	
}
